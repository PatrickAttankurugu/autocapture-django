# requirements.txt

# Core
Django==4.2.10
Pillow==10.1.0  # For image processing

# Development tools
django-debug-toolbar==4.2.0  # Optional, for debugging
django-sslserver==0.22       # Optional, for HTTPS in development

# API (if you plan to expand the verification API)
djangorestframework==3.14.0  # Optional, for building REST APIs